# contracts/

Versioned JSON Schemas this front consumes/produces. Sourced from the platform
layer; pin the version. Changing a contract = PR + version bump. Contract tests
under tests/contract_tests validate I/O against these schemas.
